{
  "name": "com.gamecore.rules.narrative",
  "version": "1.0.0",
  "displayName": "GameCore Rules Narrative",
  "description": "Pure narrative domain rules: chapter content descriptors, durable quest facts, dialogue choice validation, gate evaluation, encounter hook plans and the canonical reference trace (GC-010). Unity-free by construction; the runtime assembly's only dependency is com.gamecore.contracts, and the package's Editor-only test assembly additionally references the derivation fixtures, which the validation project's manifest supplies directly.",
  "unity": "6000.0",
  "keywords": [
    "gamecore",
    "rules",
    "narrative"
  ],
  "dependencies": {
    "com.gamecore.contracts": "1.0.0",
    "com.gamecore.derivation": "1.0.0"
  }
}
