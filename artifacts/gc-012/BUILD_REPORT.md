# GC-012 Linux build and test report

## Revision and host

- Baseline fetched/reset as requested: `35a446816420002d503feaa4a675de1715e77d17` (`origin/gc-012`). Build-host implementation fixes: `0a5614861739215b951bc7b683a5c996f08ff620`. The evidence/report commit following this one changes documentation and results only.
- Ubuntu 24.04, Linux `7.0.0-31-generic`, x86_64, Intel Core i7-12700KF, 12th Gen. GCC 13.3.0, Clang 18.1.3, GNU ld 2.42.
- .NET SDK 8.0.425, runtime 8.0.31; Unity Editor 6000.0.75f1, Linux IL2CPP module. Locked packages: Entities 1.4.6, Collections 2.6.6, Burst 1.8.28, Mathematics 1.3.2, Test Framework 1.6.0, Performance Testing 3.0.3, Linux toolchain 2.0.11.
- Managed sources retain C# 9 and .NET Standard 2.1 pure assemblies; Unity project builds StandaloneLinux64 x86_64, IL2CPP Release, Burst enabled, High managed stripping. `Assets/link.xml` has no `preserve="all"` entry for kernel or gameplay assemblies; its remaining whole-assembly entries are validation/Unity fixtures. `GameAssembly.so` SHA-256: `0cbaa0b131580ff02485d1d46f8069347df85e36d897fad89d582de10040ac5d`; IL2CPP metadata SHA-256: `e9452055165d2355a78ce2184de7ee92b92399d3b3aa59bc2f69d61daeb3695d`. The small launcher alone is not a binary identity; `toolchain/environment.txt` records its hash.
- Manifest SHA-256 `1758389a1910da9710ee7c9cafd875731813b81a54508d3445106d7efc944921`. Unity resolved `Packages/packages-lock.json` without changing it; SHA-256 `9a243cfbdb35d1d901102cd68219448f8189ba9d9dc8c2ba86f32ada22dc7597`. New `.meta` GUIDs imported unchanged; no duplicate GUIDs in validation assets.
- Production-generated catalogs are byte-identical to a fresh compiler generation (seven committed-catalog dotnet cases passed). Probe catalog full-file SHA-256 `5298bbc853b35424c7b7e8300b7c39afe6e27ec493027a22a47336a5cb567625`, registration fingerprint `02bb94ab81353a2063fb1cb92d77ce3b9889885b02a95de515193e45178f37ff`; card catalog SHA-256 `4761d745e8386cd5831005cd532ae26615c13f252ef064f45bb0888ca9e764a7`, fingerprint `77838f4766f9086a0fa32b4fb30d8d5fceeb3a8072e61594fa2177946ad3315d`.

## Commands and results

Environment for .NET: `DOTNET_ROOT=$HOME/.dotnet PATH=$HOME/.dotnet:$PATH DOTNET_CLI_TELEMETRY_OPTOUT=1`. Unity executable: `$HOME/Unity/Hub/Editor/6000.0.75f1/Editor/Unity`. Commands were run from this worktree. Unity's absolute `-projectPath`, `-logFile`, and `-testResults` paths were the corresponding paths shown below.

| Command | Final result / evidence |
| --- | --- |
| `git fetch origin && git checkout gc-012 && git reset --hard origin/gc-012` | Reset to the baseline above before edits. |
| `UNITY=$HOME/Unity/Hub/Editor/6000.0.75f1/Editor/Unity DOTNET=$HOME/.dotnet/dotnet PROBE_RUNS=5 tools/run_w4_profile_gate.sh` | **Pass end-to-end** after implementation commit `0a5614861739215b951bc7b683a5c996f08ff620`: build 0 warnings/0 errors, .NET 655/655, EditMode 613/613, PlayMode 6/6, High-stripping IL2CPP build, generated catalog diff checks, and all 45 probe executions clean. The integrated gate wrote `trx/`, which is retained alongside the earlier `trx-final/` confirmation. |
| `$HOME/.dotnet/dotnet build dotnet/GameCore.sln -c Release` | Pass, whole solution, 0 errors after compile fixes. |
| `$HOME/.dotnet/dotnet test dotnet/GameCore.sln -c Release --logger trx --results-directory artifacts/gates/w4-generic-profile/trx-final` (also rerun `--no-build`) | Pass: **655 passed, 0 failed, 0 skipped** across 11 test projects. The 11 latest TRX files are committed under `trx-final/`. |
| `python3 tools/check_game_core_csharp.py` | Pass: 272 C# files, `ok`. |
| `python3 tools/w4_generic_profile_audit.py --out artifacts/gates/w4-generic-profile/generic-profile-audit.json` | Pass: 38 assembly definitions; 14 kernel assemblies; 697 kernel types; 0 kernel→gameplay and kernel→validation edges; 0 no-engine-reference violations. |
| `Unity -batchmode -nographics -quit -projectPath unity/GameCore.Validation -logFile artifacts/gates/w4-generic-profile/unity/resolve.log` | Pass: packages resolved and scripts compiled after fixes. Lock unchanged. |
| `Unity -batchmode -nographics -quit -projectPath unity/GameCore.Validation -executeMethod GameCore.Validation.Editor.CardCatalogGenerator.GenerateCatalog -logFile artifacts/gates/w4-generic-profile/unity/card-codegen.log` | Pass; card catalog regenerated by production compiler. |
| `Unity -batchmode -nographics -quit -projectPath unity/GameCore.Validation -executeMethod GameCore.Validation.Editor.ProbeCatalogGenerator.GenerateCatalog -logFile artifacts/gates/w4-generic-profile/unity/probe-codegen.log` | Pass; probe catalog regenerated. |
| `python3 tools/verify_generated_catalog.py <ProbeCatalog.g.cs>` and separately `<CardCatalog.g.cs>` | Pass: respectively 3 groups/4 factories/1 schema and 5 groups/9 factories/1 schema; generated file-prefix hashes and catalog fingerprints recomputed. |
| `Unity -batchmode -nographics -projectPath unity/GameCore.Validation -runTests -testPlatform EditMode -testResults artifacts/gates/w4-generic-profile/unity/editmode-results.xml -logFile artifacts/gates/w4-generic-profile/unity/editmode.log` | Pass: **613 passed, 0 failed, 0 skipped**, 14 test assemblies including W1–W4, narrative, cards, planner, runtime. The new expected-domain-version regression passed. |
| `Unity -batchmode -nographics -projectPath unity/GameCore.Validation -runTests -testPlatform PlayMode -testResults artifacts/gates/w4-generic-profile/unity/playmode-results.xml -logFile artifacts/gates/w4-generic-profile/unity/playmode.log` | Pass: **6 passed, 0 failed, 0 skipped**, one adapter assembly. |
| `UNITY=$HOME/Unity/Hub/Editor/6000.0.75f1/Editor/Unity DOTNET=$HOME/.dotnet/dotnet ARTIFACTS=$PWD/artifacts/gates/w4-generic-profile/toolchain tools/unity/build_probe.sh` | Pass: production probe catalog regeneration and StandaloneLinux64 IL2CPP Release player with High stripping; `toolchain/{codegen.log,build.log,environment.txt}`. |
| `PROBE_RUNS=5 ARTIFACTS=$PWD/artifacts/gates/w4-generic-profile/toolchain PROBE_PLAYER=$PWD/unity/GameCore.Validation/Builds/Linux64/GameCoreProbe.x86_64 tools/unity/run_probe.sh both` | Pass: positive **5/5**, deliberately missing registration **5/5 ExpectedNegative** (exit 3, not a failed run). |
| Same environment, `tools/unity/run_world_probe.sh` | Pass **5/5**. |
| Same environment, `tools/unity/run_w1_gate_probe.sh` | Pass **5/5**. |
| Same environment, `tools/unity/run_w2_gate_probe.sh` | Pass **5/5**. |
| Same environment, `tools/unity/run_narrative_probe.sh` | Pass **5/5**. |
| Same environment, `tools/unity/run_cards_probe.sh` | Pass **5/5**. |
| Same environment, `tools/unity/run_w3_gate_probe.sh` | Pass **5/5**. |
| Same environment, `tools/unity/run_w4_profile_probe.sh` | Pass **5/5**; seven W4 steps pass in each run. Both generated inactive entries resolve, narrative and card traces agree against their Editor/world fixtures, and a live `Additive` row composes +2/+3 to 5 with two support rows. Retracting the nested provider leaves value 2 and one support row. Player-loaded kernel audit: zero forbidden references, duplicate assemblies and inspection failures. |
| `python3 tools/validate_game_core_docs.py --self-test && python3 tools/validate_game_core_docs.py` | Pass: nine validator fixtures and all 14 design documents. |

All 45 final player process JSON results are retained as `toolchain/probe-*.json[.run2..run5]`, along with their headless player logs. Results: 40 `Pass`, five deliberate `ExpectedNegative`, zero failed/blocked final runs. `-batchmode -nographics` was used throughout, with `-quit` only on resolve/generation/build (never on `-runTests`). The W4 player starts with one **application bootstrap** world (`registryAtStart=1`, `startupWorldIsBootstrap=True`) before `AfterSceneLoad`; neither family is mounted there. After the family scenarios, `registryAfterAll=0`. EditMode starts at zero. This corrects the handoff's inaccurate `registryAtStart=0` player expectation rather than hiding the existing bootstrap world.

## Fixes and actual failed attempts

1. Initial .NET build failed in `CapabilitySupport` (missing equality-consistent hash) and `AssemblyPlanner` (`supports` local shadowed the support-record list). Added `GetHashCode` and renamed the binding-row local. No contract semantics changed.
2. Unity resolve initially failed: `AssemblyPublisher` had duplicate `ToBinding`/`RowOf` implementations; the stale versions omitted support sets. Removed them. Generated catalogs referenced entry types in `ProbeHost`, creating an assembly cycle; moved the unchanged entry source to an independent `GameCore.Validation.FamilyEntries` asmdef, updated referencing asmdefs, and imported stable `.meta` files. Fixed missing fixture/scenario imports and wrong W4 test member/type names.
3. Initial dotnet tests: 652 passed, three failed in the content-compiler project. Both committed catalogs had an extra newline versus the authoritative production emitter; regenerated both byte-identically in Unity. The card literal test asserted the old incidental count of eight even though the new declared entry made nine; removed that count assertion, kept every derived-key check and fresh-generation comparison. No expected gameplay values were changed.
4. First Unity EditMode run: 593 passed, 19 failed. Adding a second buffer invalidated a previously obtained Unity Entities `DynamicBuffer` handle during spawn. Added both buffers before taking handles. `InstallBindingRow` now counts support-row writes, so the postwrite fault injection using `writes == 1` no longer fired; changed it to the first transition from zero to positive writes. The prior spawn and fault tests then passed.
5. Next EditMode runs exposed the new fixture's no-target-change epoch gap and missing nested declaration in its hand-written catalog. Published an unchanged assembly when the lane advanced without target changes and used the full additive declaration set for both catalogs. Cross-catalog equality now excludes only their deliberately different catalog fingerprint prefixes; all remaining observed facts, including support rows and retraction, must match. Kept the `+2 +3 = 5` and supporter expectations intact.
6. Initial IL2CPP W4 probe failed 5/5 despite both families' runtime checks passing: the player already had one owned application bootstrap world, and the driver looked for additive details in the brief step rather than its full facts digest. The gate now verifies the bootstrap world's identity, absence of family mounts before scenario execution, and zero worlds after teardown; the driver reads support-row details from the facts. Narrative catalog fingerprint is named explicitly in the facts. Rebuilt and reran W4 5/5 clean.
7. An additional guarded-command regression exposed a real idempotency bug: `RequestLedger` detected a reused request key with a different expected-domain-version hash, but `WorldMessagePlane` returned the *prior* `StalePlan` outcome instead of `IdempotencyConflict`. The refusal receipt now reports its own conflict reason; the retained prior row remains unchanged. The focused regression and final full EditMode suite pass. No test was weakened or skipped. One Unity full-suite invocation stalled during editor initialization before executing tests; it was terminated, and the following isolated and full suite runs completed cleanly.
8. Corrected `audit.md`, `inventory.{md,json}` and `profile.md` from authoring-host `NotRun` to the actual run evidence. Inventory still conservatively counts **35 Implemented+Evidenced / 47 Partial / 4 Not yet** over all 86 IDs. `P-017` stays Partial (ContributionId reconfiguration stability and the full TEST-005 matrix remain open). No untested product platform, action family, cross-family composition, checkpoint/recovery, or measured budget is claimed.

## Scope and residual risk

The runner did not prove a final conformance freeze. Card canonical comparison is between generated and fixture world facts (normalizing only the distinct catalog identity), while narrative also checks its committed trace. The domain-version stale-envelope refusal is directly evidenced by EditMode; player runs exercise both bound families but do not inject a stale guard. The historical once-observed player exit-139 native teardown fault did **not** recur in the final 45 integrated-gate runs; its original cause remains unknown. The **integrated** `tools/run_w4_profile_gate.sh` completed successfully after source catalogs were committed, including its `git diff --exit-code` reproducibility checks. Earlier individual runs found and repaired the defects above.
