# GC-017 Linux build and test report

Host: Ubuntu 24.04, Linux x86_64; .NET SDK 8.0.425/runtime 8.0.31; Unity 6000.0.75f1 (activated Personal license), StandaloneLinux64 IL2CPP Release, High stripping, C# 9, .NET Standard 2.1. Started from `git fetch origin && git checkout gc-017 && git reset --hard origin/gc-017` at `c3ddc13`. Build evidence refers to the fixes in this worktree.

## Commands and observed results

Working directory: repository root. Environment: `DOTNET_ROOT=$HOME/.dotnet PATH=$HOME/.dotnet:$PATH DOTNET_CLI_TELEMETRY_OPTOUT=1`; `UNITY=$HOME/Unity/Hub/Editor/6000.0.75f1/Editor/Unity`.

| Command | Result | Evidence |
| --- | --- | --- |
| `$HOME/.dotnet/dotnet build dotnet/GameCore.sln -c Release` | Pass | Console output; 11 test projects subsequently ran |
| `$HOME/.dotnet/dotnet test dotnet/GameCore.sln -c Release --logger trx --results-directory artifacts/faults/trx` | Pass: **750/750**, 0 failed, 0 skipped, 11 projects | `artifacts/faults/trx/*.trx` (one latest result per project; the equivalent `--no-build` repeat also passed) |
| `timeout --signal=TERM --kill-after=60 1800 "$UNITY" -batchmode -nographics -quit -projectPath "$PWD/unity/GameCore.Validation" -logFile "$PWD/artifacts/faults/unity/resolve.log"` | Pass after compiler fixes | `artifacts/faults/unity/resolve.log` |
| Same bounded Unity invocation, without `-quit`, with `-runTests -testPlatform EditMode -testResults "$PWD/artifacts/faults/unity/editmode-results.xml" -logFile "$PWD/artifacts/faults/unity/editmode.log"` | Pass: **807/807**, 0 failed/skipped, 20 test assemblies | XML and log |
| Same bounded Unity invocation with `-runTests -testPlatform PlayMode -testResults "$PWD/artifacts/faults/unity/playmode-results.xml" -logFile "$PWD/artifacts/faults/unity/playmode.log"` | Pass: **6/6**, 0 failed/skipped, one test assembly | XML and log |
| `UNITY="$UNITY" ARTIFACTS="$PWD/artifacts/faults/toolchain" UNITY_TIMEOUT=1800 tools/unity/build_probe.sh` | Pass: generated catalog; IL2CPP StandaloneLinux64/High player | `toolchain/codegen.log`, `toolchain/build.log`, `toolchain/environment.txt` |
| `PROBE_RUNS=5 ARTIFACTS="$PWD/artifacts/faults/toolchain" tools/unity/run_gc017_faults_probe.sh` | Pass: **62/62** observations on **each of five** separate player processes; 310 passing executions, 0 failing | `probe-gc017-faults.json[.run2..run5]`, player logs |
| `PROBE_RUNS=5 ARTIFACTS="$PWD/artifacts/faults/toolchain" tools/unity/run_{probe,world_probe,w1_gate_probe,w2_gate_probe,narrative_probe,cards_probe,w3_gate_probe,w4_profile_probe,gc013_probe,w4_gate_probe}.sh` (expanded and run as ten individual scripts) | Pass: all ten prior probe scripts, each at five repetitions; baseline `run_probe.sh` also exercised its expected-negative path five times | `toolchain/probe-*.json*` and player logs |
| `python3 tools/check_game_core_csharp.py` | Pass: 371 C# files | Console output |
| `python3 tools/validate_game_core_docs.py --self-test`; `python3 tools/validate_game_core_docs.py` | Pass: 9 fixtures, 14 documents | `validator-self-test.log`, `validator.log` |

The same suite was run during debugging: first EditMode 797/807, then 799/807, then 806/807; fixes yielded 807/807. Earlier failing XML was replaced by the final successful result, not represented as a pass. PlayMode passed both before and after the qualification switch. The prior probe scripts passed after the final IL2CPP rebuild. Every Unity/player invocation above was bounded by `timeout` (1800 s editor, 600 s player); no observed timeout or 10-minute no-progress hang.

## TEST-009 / TEST-016 / TEST-018 boundary evidence

`artifacts/faults/boundaries.json` maps **29/29 passing cases** to the ten named TEST-016 matrix rows. The 62-case player result runs the 15 frozen family observations over both generated and generated-style catalogs for narrative and cards, plus both digest checks. Specifically: validation/acquisition/fence/migration refusals preserve the old image and prevent writes; the narrative migration case plans a real registered 1→2 slot migration and faults before scratch apply; cards has no declared version-change migration, so its case proves only the correctly placed prewrite latch (no migrated slot is claimed). First-live-write and gate-installation faults latch the publisher's world, not the execution driver, and keep only the last published observation. Structural playback faults latch the driver, stop step commit and retain resources until safe teardown. Before-cutoff cancellation reports Cancelled, after-cutoff TooLate; cleanup refusal retains staged ownership; recovery from initial definitions creates a fresh world and rejects the old callback. The EditMode guarded-dispatch cases include the stock-group-swallowing contrast and output-after-commit behavior. No unsafe release was observed in these cases. **Checkpoint-based restore/recovery is NotRun/NotImplemented for GC-017** (GC-018/GC-027 own it); the row-10 promotion is explicitly only the initial-definition repair-failure case. TEST-018's ten domain-reload-on/off cycles were not separately exercised; the six PlayMode cases passed, not the full broader TEST-018 specification.

## Fixes

1. `AssemblyPublisher.Publish`: define `nextEpoch` and `nextRevision` from the adopted publication before stamping/committing; missing locals blocked Unity compilation.
2. `UnityExecutionDriver`: supply an `OperationId` rather than `WorldId` to the structural fault record (world/session/step-derived identity); Unity compiler rejected the old argument.
3. `InitialDefinitionRecovery`: import the existing `GameCore.Execution.GameCoreThreading` namespace; absent import blocked Unity compilation.
4. `FaultScenario`: qualify `global::Unity.Entities.Entity` and join the `IReadOnlyList<string>` through `IEnumerable<string>`; both were Unity compile errors. Corrected provenance assertion to name the derivation operation used by the publisher. Corrected migration fixture to use an actually declared/registered migration where available, avoiding fabricated schema-0 slots that made the planner reject before the latch. Corrected postwrite host-vs-driver fault assertions and retained-resource expectation for a faulted-but-not-yet-stopped world. All 15 names/digests remain unchanged.
5. `RecoveryFixture`: second publication now reads actual current live slot schemas after first publication migrated 1→2, rather than reusing stale schema-1 seeded input. Kept the first migration case. Changed absolute fault reach expectation to a delta, since the first successful publication already reached the unarmed boundary. `InitialDefinitionRecoveryTests` now checks recovery adds no reaches to that baseline rather than incorrectly asserting one lifetime reach. No expected protocol value changed.
6. Probe script: C# boolean detail prints `True`, not lowercase `true`; corrected the script's fragment so it verifies the actual recorded successful lease release rather than rejecting five passing player results.
7. Release switch: `com.unity.test-framework` is brought in **transitively** by Unity packages, so the original asmdef version define remained enabled even after removing direct test dependencies. Added an explicit `com.gamecore.fault-qualification` marker package only to the validation project's manifest, and switched the runtime asmdef define to that marker; updated error text. Unity regenerated `Packages/packages-lock.json`.

No tests were ignored, skipped, deleted or weakened to pass. `artifacts/gates/w4-generic-profile/inventory.json` was not promoted: this job produced fault evidence, not that inventory's generic-profile measurements.

## Shipping build limit: latch exclusion is incomplete

A disposable Unity project cloned from validation assets/settings/catalog removed the marker and both direct test-framework dependencies. Unity still resolved Test Framework **transitively**; the rebuilt runtime compiler `.rsp` nonetheless omitted `GAMECORE_FAULT_INJECTION`. A separate StandaloneLinux64 IL2CPP Release/High player built and ran the baseline probe **8/8 Pass**; see `toolchain/release-build.log`, `toolchain/release-player.json`, `toolchain/release-player.log`, `toolchain/release-define-evidence.txt`. Generated C++ for `FaultCompilation.IsCompiledIn`, `FaultReach.Reach` and `FaultReach.Refuse` simply returned false; the validation compiler `.rsp` did contain the marker define. **However, generated C++ still contains empty fault-reach functions and metadata, `AssemblyFaultInjection`, and per-world latch allocation/arrays.** Thus the requested stronger claim—**no latch code or cost in the stripped player**—is **Fail / not proven**. Disabled reaches have no effect but neither complete binary absence nor zero per-world allocation is established. This is an outstanding acceptance defect, not a claimed pass. The disposable release project/build output was removed after inspection; compact textual logs and the 8-case JSON were retained.

## Round 2 — guarded fault latches and inspected release player

Started at `43bfbb4` after `git fetch origin && git checkout gc-017 && git reset --hard origin/gc-017`. Same Ubuntu x86_64 host, .NET SDK 8.0.425 and Unity 6000.0.75f1. Set `DOTNET_ROOT=$HOME/.dotnet`, prefixed `PATH` with `$HOME/.dotnet`, and set `DOTNET_CLI_TELEMETRY_OPTOUT=1`. The standalone build entry point selects IL2CPP Release, High stripping and .NET Standard 2.1; the release runtime compiler responses use `-langversion:9.0` and do not define `GAMECORE_FAULT_INJECTION`.

| Exact command (from repository root; paths below relative unless stated) | Round-2 result | Evidence |
| --- | --- | --- |
| `UNITY=$HOME/Unity/Hub/Editor/6000.0.75f1/Editor/Unity DOTNET=$HOME/.dotnet/dotnet PROBE_RUNS=5 tools/run_gc017_gate.sh` | First attempt: dotnet and Unity suites plus player probe passed; release-surface checker **Fail**, caused by its incorrect byte scan, not a failing runtime case. | Initial `artifacts/faults/release-surface.json` replaced by corrected check. |
| `python3 tools/check_release_fault_free.py --dotnet "$HOME/.dotnet/dotnet" --json artifacts/faults/release-surface.json` | Pass after checker fix. | `artifacts/faults/release-surface.json` |
| `python3 tools/unity/prepare_gc017_release_project.py`; `UNITY=$HOME/Unity/Hub/Editor/6000.0.75f1/Editor/Unity UNITY_PROJECT="$PWD/unity/GameCore.ReleaseCheck" ARTIFACTS="$PWD/artifacts/faults/release" UNITY_TIMEOUT=1800 tools/unity/build_probe.sh` | Pass: separate marker-free StandaloneLinux64 IL2CPP Release/High player; codegen and player build both complete. The disposable project uses the same local package paths and has no marker, direct test framework dependency, testables or fault-only probe fixture. The build script wraps each Editor invocation in `timeout 1800`. | `artifacts/faults/release/{codegen.log,build.log,environment.txt}`; `unity/GameCore.ReleaseCheck/Packages/manifest.json` is generated and ignored. |
| `python3 tools/check_player_fault_free.py --player unity/GameCore.ReleaseCheck/Builds/Linux64 --il2cpp unity/GameCore.ReleaseCheck/Library/Bee/artifacts/LinuxPlayerBuildProgram/il2cppOutput/cpp --json artifacts/faults/release-player-surface.json` | Pass: 73 managed assemblies and 690 generated C++/header files scanned; **zero** latch types, boundary names or trace prefixes. | `artifacts/faults/release-player-surface.json` |
| `PROBE_PLAYER="$PWD/unity/GameCore.ReleaseCheck/Builds/Linux64/GameCoreProbe.x86_64" UNITY_PROJECT="$PWD/unity/GameCore.ReleaseCheck" PROBE_RUNS=5 ARTIFACTS="$PWD/artifacts/faults/release" tools/unity/run_narrative_probe.sh` and identically `tools/unity/run_cards_probe.sh` | Pass: narrative **24/24 × 5**; cards **28/28 × 5**; zero failed, skipped or not run. Every player process was bounded by `timeout 600`. | `artifacts/faults/release/probe-{narrative,cards}.json[.run2..run5]` and player logs. |
| `RELEASE_PLAYER="$PWD/unity/GameCore.ReleaseCheck/Builds/Linux64" RELEASE_IL2CPP="$PWD/unity/GameCore.ReleaseCheck/Library/Bee/artifacts/LinuxPlayerBuildProgram/il2cppOutput/cpp" UNITY=$HOME/Unity/Hub/Editor/6000.0.75f1/Editor/Unity DOTNET=$HOME/.dotnet/dotnet PROBE_RUNS=5 tools/run_gc017_gate.sh` | **Pass end to end**: .NET solution build 0 warnings/errors; 11 test projects **750/750**; Unity resolve Pass; EditMode **807/807** and PlayMode **6/6**; qualification IL2CPP player build Pass; fault probe **62/62 × 5**; release source/assembly check Pass; actual release-player inspection Pass; documentation validator 9 fixtures and 14 documents Pass. Zero failures, skips or inconclusives in test suites. | `artifacts/faults/trx/*.trx`, `artifacts/faults/unity/{resolve.log,editmode-results.xml,playmode-results.xml}`, `artifacts/faults/toolchain/{codegen.log,build.log,probe-gc017-faults.json[.run2..run5]}`, and both `release-*-surface.json`. |

`artifacts/faults/boundaries.json` maps 29 TEST-016 cases to **14/14 passed EditMode cases** and **15/15 passed player observations** in the last gate; all five qualification-player runs had 62 passing observations. The release manifest lacks `com.gamecore.fault-qualification`; both runtime compiler `.rsp` files lack `GAMECORE_FAULT_INJECTION` and use C# 9. The inspected generated output has no latch metadata, per-world latch allocation or reach call sites: their named types and reach methods do not occur in the inspected C++ or managed assemblies. This is an artifact-level absence check, not a runtime allocation profiler or full machine-code disassembly.

Round-2 fixes: `tools/check_release_fault_free.py` had treated the assembly name `GameCore.Faults.ReleaseCheck` as a leaked `Fault` type and searched UTF-8 for C# string literals stored as UTF-16LE. It now checks actual latch type names and both encodings of boundary strings and the trace prefix; its compiled-release/qualification checks pass. `tools/check_player_fault_free.py` printed Pass but wrote `"status": "Fail"` into the JSON on success; it now assigns verdict before serialization. Added `tools/unity/prepare_gc017_release_project.py` and ignored its disposable output; the recipe reproduces the tested manifest and release-only edited C# sources byte-for-byte. No implementation or test expectations changed. The prior section's incomplete-shipping-latch finding describes the pre-round-2 revision and is **superseded** by this inspected round-2 release player. Checkpoint-based recovery and the broader TEST-018 domain-reload cycle matrix remain outside the GC-017 runs; no such pass is claimed.
